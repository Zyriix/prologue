# ============================================================================
# AR Model: XLarge (dim=1280, 32 layers, 20 heads, ~685M)
# head_dim = 1280/20 = 64 = sum(rope_axes_dim=[24,20,20])
# ============================================================================

ARModel:
    input_dim: 1280
    dim: 1280
    layer_num: 32
    heads: 20
