# ============================================================================
# AR Model: Small (dim=512, 10 layers, 8 heads)
# ============================================================================

ARModel:
    dim: 512
    layer_num: 10
    heads: 8
    tied_embedding: True
