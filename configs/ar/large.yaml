# ============================================================================
# AR Model: Large (dim=1024, 24 layers, 16 heads)
# ============================================================================

ARModel:
    input_dim: 1024
    dim: 1024
    layer_num: 24
    heads: 16
